import React from "react";
import "../Styles/SpinnerLoader.css"; // Import the CSS file

const SpinnerLoader = () => {
    return (
        <div className="spinner-container">
            <div className="spinner"></div>
        </div>
    );
};

export default SpinnerLoader;
